import { OPTIONS } from '@barely/utils';

import { invoiceRenderHandler } from '@barely/api/public/invoice-render.handler';

export { OPTIONS, invoiceRenderHandler as GET, invoiceRenderHandler as POST };
