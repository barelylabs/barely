import { OPTIONS } from '@barely/utils';

import { providerAccountHandler } from '@barely/api/app/sub/provider-account.handler';

export { OPTIONS, providerAccountHandler as GET, providerAccountHandler as POST };
