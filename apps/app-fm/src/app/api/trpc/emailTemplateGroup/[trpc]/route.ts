import { OPTIONS } from '@barely/utils';

import { emailTemplateGroupHandler } from '@barely/api/app/sub/email-template-group.handler';

export { OPTIONS, emailTemplateGroupHandler as GET, emailTemplateGroupHandler as POST };
