import { OPTIONS } from '@barely/utils';

import { analyticsEndpointHandler } from '@barely/api/app/sub/analytics-endpoint.handler';

export { OPTIONS, analyticsEndpointHandler as GET, analyticsEndpointHandler as POST };
