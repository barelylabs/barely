import { OPTIONS } from '@barely/utils';

import { emailTemplateHandler } from '@barely/api/app/sub/email-template.handler';

export { OPTIONS, emailTemplateHandler as GET, emailTemplateHandler as POST };
