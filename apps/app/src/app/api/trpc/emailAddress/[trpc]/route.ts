import { OPTIONS } from '@barely/utils';

import { emailAddressHandler } from '@barely/api/app/sub/email-address.handler';

export { OPTIONS, emailAddressHandler as GET, emailAddressHandler as POST };
