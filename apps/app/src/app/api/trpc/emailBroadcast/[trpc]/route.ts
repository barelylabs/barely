import { OPTIONS } from '@barely/utils';

import { emailBroadcastHandler } from '@barely/api/app/sub/email-broadcast.handler';

export { OPTIONS, emailBroadcastHandler as GET, emailBroadcastHandler as POST };
