import { OPTIONS } from '@barely/utils';

import { invoiceClientHandler } from '@barely/api/app/sub/invoice-client.handler';

export { OPTIONS, invoiceClientHandler as GET, invoiceClientHandler as POST };
