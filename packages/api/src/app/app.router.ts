import type {
	AppRouter,
	AppRouterContext,
	AppRouterInputs,
	AppRouterKeys,
	AppRouterOutputs,
} from '@barely/lib/trpc/app.route';
import { appRouter } from '@barely/lib/trpc/app.route';

export {
	appRouter,
	type AppRouter,
	type AppRouterInputs,
	type AppRouterOutputs,
	type AppRouterContext,
	type AppRouterKeys,
};
